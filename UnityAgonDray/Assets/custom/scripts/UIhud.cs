using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class UIhud : MonoBehaviour
{
    int keys;
    int maxKeys = 3;

    public TMP_Text keysDisplay;
    
    
    // Start is called before the first frame update
    void Start()
    {
        //gm = GameManager.GM;
        //need to make GM
    }

    // Update is called once per frame
    void Update()
    {
        //update the keys display
        //keys = gm.keys;
        //if (keysDisplay) { keysDisplay.text = "Keys: " + keys + " /" + maxKeys; }
        //GetGameStats();
        //SetHUD();
    }
    


}
